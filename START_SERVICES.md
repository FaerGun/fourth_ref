# 🚀 Инструкция по запуску сервисов

## Быстрый запуск

```bash
# 1. Перейдите в директорию проекта
cd "C:\Users\shved\Desktop\refuck\forth\he-path-of-the-samurai"

# 2. Запустите все сервисы
docker-compose up -d

# 3. Дождитесь инициализации (1-2 минуты)
docker-compose logs -f
```

Нажмите `Ctrl+C` чтобы выйти из просмотра логов, когда увидите:
- `[php] starting php-fpm`
- `rust_iss listening on 0.0.0.0:3000`

## Проверка статуса

```bash
docker-compose ps
```

Все сервисы должны быть в статусе `Up` или `Up (healthy)`:

```
NAME              STATUS
iss_db            Up (healthy)
redis_cache       Up (healthy)
rust_iss          Up
php_web           Up
web_nginx         Up
legacy_telemetry  Up
```

## Открытие в браузере

После успешного запуска откройте:

- 🌐 **Главная страница:** http://localhost:8080/
- 📊 **Dashboard:** http://localhost:8080/dashboard
- 🔧 **Rust API Health:** http://localhost:8081/health

## Если видите ошибку 404

1. **Убедитесь, что сервисы запущены:**
   ```bash
   docker-compose ps
   ```

2. **Проверьте логи PHP:**
   ```bash
   docker-compose logs php --tail 50
   ```
   
   Должно быть: `[php] starting php-fpm`

3. **Проверьте логи nginx:**
   ```bash
   docker-compose logs nginx --tail 20
   ```

4. **Подождите 1-2 минуты** - Laravel создается при первом запуске

5. **Если проблема сохраняется, пересоберите:**
   ```bash
   docker-compose down
   docker-compose build --no-cache php
   docker-compose up -d
   ```

## Просмотр логов в реальном времени

```bash
# Все сервисы
docker-compose logs -f

# Конкретный сервис
docker-compose logs -f php
docker-compose logs -f rust_iss
docker-compose logs -f nginx
```

## Остановка сервисов

```bash
# Остановка с сохранением данных
docker-compose stop

# Остановка и удаление контейнеров
docker-compose down

# Полная очистка (удалит данные!)
docker-compose down -v
```

## Полезные команды

```bash
# Перезапуск конкретного сервиса
docker-compose restart php

# Пересборка и перезапуск
docker-compose up -d --build

# Проверка конфигурации
docker-compose config

# Использование ресурсов
docker stats
```

## Время инициализации

При первом запуске:
- **PostgreSQL:** ~10 секунд
- **Redis:** ~5 секунд
- **Rust сервис:** ~2-3 минуты (компиляция)
- **PHP/Laravel:** ~1-2 минуты (composer install)
- **Legacy сервис:** ~10 секунд

**Общее время:** 3-5 минут при первом запуске.

При последующих запусках: ~30 секунд.


